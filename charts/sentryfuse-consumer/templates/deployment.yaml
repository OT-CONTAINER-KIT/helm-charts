---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "base.fullname" . }}
  namespace: sentryfuse
  labels:
    {{- include "sentryfuse.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount | default 1 }}
{{- end }}
  selector:
    matchLabels:
      {{- include "sentryfuse.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "sentryfuse.labels" . | nindent 8 }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.base.image.pullSecrets }}
      terminationGracePeriodSeconds: 120
      containers:
        - name: {{ include "base.fullname" . }}
          image: "{{ .Values.base.image.repository }}:{{ .Values.base.image.tag }}"
          imagePullPolicy: {{ .Values.base.image.pullPolicy | default "IfNotPresent" }}

          {{- if .Values.base.image.args }}
          args:
            {{- toYaml .Values.base.image.args | nindent 12 }}
          {{- end }}

          ports:
            - name: http
              containerPort: {{ .Values.service.port | default 8080 }}
              protocol: TCP

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          env:
            - name: FORCE_ROLLOUT
              value: {{ now | quote }}
          {{- range $key, $value := .Values.base.config }}
            - name: {{ $key }}
              value: "{{ $value }}"
          {{- end }}
          {{- if .Values.base.existingSecrets }}
          envFrom:
            {{- range .Values.base.existingSecrets }}
            - secretRef:
                name: {{ . }}
            {{- end }}
          {{- end }}

          {{- if .Values.healthcheck.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.healthcheck.statusPath }}
              port: http
            initialDelaySeconds: 30
            periodSeconds: 60
            successThreshold: 1
            timeoutSeconds: 1
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: {{ .Values.healthcheck.statusPath }}
              port: http
            initialDelaySeconds: 30
            periodSeconds: 60
            successThreshold: 1
            timeoutSeconds: 1
            failureThreshold: 3
          {{- end }}

          {{- if .Values.volumes }}
          volumeMounts:
            {{- if .Values.volumes.configMaps }}
            {{- range $conf := .Values.volumes.configMaps }}
            - mountPath: {{ $conf.mountPath }}
              name: {{ $conf.name }}-volume
            {{- end }}
            {{- end }}
          {{- end }}

      {{- if .Values.volumes }}
      volumes:
        {{- if .Values.volumes.configMaps }}
        {{- range $conf := .Values.volumes.configMaps }}
        - name: {{ $conf.name }}-volume
          configMap:
            name: {{ $conf.name }}
        {{- end }}
        {{- end }}
      {{- end }}
