apiVersion: v1
kind: Service
metadata:
  name: {{ include "vault.fullname" . }}-transit-headless
spec:
  clusterIP: None
  selector:
    app: {{ include "vault.fullname" . }}-transit
  ports:
    - port: 8200
      name: http
    - port: 8201
      name: internal