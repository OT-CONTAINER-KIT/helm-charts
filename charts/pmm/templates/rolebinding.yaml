apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: {{ include "pmm.fullname" . }}

subjects:

- kind: ServiceAccount

  name: {{ include "pmm.fullname" . }}

roleRef:

  apiGroup: rbac.authorization.k8s.io

  kind: Role

  name: {{ include "pmm.fullname" . }}
