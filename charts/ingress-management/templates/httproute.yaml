---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  annotations:
{{- with .Values.annotations }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  parentRefs:
{{- range .Values.parentRefs }}
  - name: {{ .name }}
{{- end }}
  hostnames:
{{- range .Values.hostnames }}
  - "{{ . }}"
{{- end }}
  rules:
  - matches:
{{- range .Values.paths }}
    - path:
        type: {{ .type }}
        value: {{ .value | quote }}
{{- end }}
    backendRefs:
{{- range .Values.backendRefs }}
    - name: {{ .name }}
      kind: {{ .kind }}
      port: {{ .port }}
{{- end }}
