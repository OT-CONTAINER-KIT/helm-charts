{{- $p := .Values.plugins | default dict }}

{{- if $p.opentelemetry }}

apiVersion: configuration.konghq.com/v1
kind: KongPlugin

metadata:
  name: opentelemetry

plugin: opentelemetry

config:
  endpoint: {{ .Values.observability.otel.endpoint | quote }}

{{- end }}
