apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kong-proxy
spec:
  type: ClusterIP
  selector:
    app: kong
  ports:
    - name: proxy
      port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kong-admin
spec:
  type: ClusterIP
  selector:
    app: kong
  ports:
    - name: admin
      port: 8001
      targetPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kong-manager
spec:
  type: ClusterIP
  selector:
    app: kong
  ports:
    - name: manager
      port: 8002
      targetPort: 8002
