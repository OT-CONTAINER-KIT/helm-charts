app:
 name: nginx-app
replicaCount: 3

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

httpPort: 80
protocol: TCP

service:
  port: 80

configMap:
  my-config-key: my-config-value

secret:
  my-secret-key: my-secret-value
