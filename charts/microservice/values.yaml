replicaCount: 1

fullnameOverride: "nginx-svc"
nameOverride: ""
image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 80         # Example valid port number
  targetPort: 8000 # Example valid target port number
  protocol: TCP    # Example protocol (usually TCP for web services)
