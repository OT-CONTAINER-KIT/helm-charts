
labels:
  app: micro

selectorLabels:
  app: microservice
  release: stable

replicaCount: 3

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

protocol: TCP

service:
  port: 80

configMap:
  my-config-key: nginx-config-value

secret:
  my-secret-key: nginx-secret-value
