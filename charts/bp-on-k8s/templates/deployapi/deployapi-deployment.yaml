apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{  .Values.deployapi.name }}
  labels: 
    app.kubernetes.io/name: {{ .Values.deployapi.name | default .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    {{- include "global.labels" .Values.deployapi | indent 4 }}
spec:
  {{- if not .Values.deployapi.hpa.enabled }}
  replicas: {{ .Values.deployapi.replicas }}
  {{- end }}  
  selector:
    matchLabels:
      {{- include "global.labels" .Values.deployapi | indent 6 }}
  template:
    metadata:
      labels:
        {{- include "global.labels" .Values.deployapi | indent 8 }}
    spec:
      {{- if ne .Values.commonServiceAccount "" }}
      serviceAccountName: {{ .Values.commonServiceAccount }}
      {{- end }}
      securityContext:
        runAsUser: {{ .Values.common.securityContext.runAsUser }}
        runAsGroup: {{ .Values.common.securityContext.runAsGroup }}
        fsGroup: {{ .Values.common.securityContext.fsGroup }}
        {{- if .Values.common.securityContext.fsGroupChangePolicy }}
        fsGroupChangePolicy: "{{- .Values.common.securityContext.fsGroupChangePolicy -}}"
        {{- end }}
        {{- if .Values.common.securityContext.supplementalGroups }}
        supplementalGroups:
          {{- range $supplementalGroup := .Values.common.securityContext.supplementalGroups }}
        - {{- $supplementalGroup -}}
          {{- end }}
        {{- end }}
      {{- include "workload.imagePullSecrets" .Values | nindent 6 }}
      containers:
        - tty: true
          env:
          - name: NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: PYTHONUNBUFFERED
            value: "1"
          - name: HOME_PATH_OF_HOST
            value: {{ .Values.envJson.homeDirectoryForSteps }}
          {{- if .Values.optimizedRun.firstTime }}
          - name: RECURSIVE_FILE_PERMISSION
            value: "1"
          {{- end }}
          image: "{{- .Values.imagePullSecrets.dockerConfig.registry -}}/{{- .Values.deployapi.image.name -}}:{{- .Values.deployapi.image.tag -}}"
          imagePullPolicy: {{ .Values.deployapi.imagePullPolicy }}
          name: {{  .Values.deployapi.name }}
          securityContext:
            allowPrivilegeEscalation: false
            # capabilities:
            #   drop:
            #   - ALL
          {{- include "workload.resources" .Values.deployapi | indent 10 }}
          {{- include "workload.startupProbe" .Values.deployapi | indent 10 }}
          {{- include "workload.livenessProbe" .Values.deployapi | indent 10 }}
          {{- include "workload.readinessProbe" .Values.deployapi | indent 10 }}
          volumeMounts:
            {{- include "workload.volumeMounts" .Values.deployapi | indent 12 }}
      {{- if .Values.deployapi.nodeSelector.enabled }}
      nodeSelector:
        {{- toYaml .Values.deployapi.nodeSelector.labels | nindent 8 }}
      {{- end }}
      restartPolicy: Always
      volumes:
        {{- include "workload.volumes" .Values.deployapi | indent 8 }}
