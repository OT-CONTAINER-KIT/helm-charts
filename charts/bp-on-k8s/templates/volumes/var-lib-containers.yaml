{{- if .Values.volumes.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.volumes.varLibContainers.name }}
  labels: 
    app.kubernetes.io/name: {{ .Values.volumes.varLibContainers.name | default .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    {{- include "global.labels" .Values.volumes.varLibContainers | indent 4 }}
spec:
  {{- if or .Values.storageClass.name .Values.volumes.varLibContainers.storageClassName }}
  storageClassName: {{ default .Values.storageClass.name .Values.volumes.varLibContainers.storageClassName }}
  {{- end }}
  accessModes:
    {{- include "volume.volumeAccessModes" .Values.volumes.varLibContainers | indent 4 }}
  resources: {{- include "volume.volumeResources" .Values.volumes.varLibContainers | nindent 4 }}
{{- end }}
