{{- if .Values.maturityDashboard.enabled }}
  {{- if .Values.maturityDashboardFrontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{  .Values.maturityDashboardFrontend.service.name }}
  labels: 
    app.kubernetes.io/name: {{ .Values.maturityDashboardFrontend.service.name | default .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    {{- include "global.labels" .Values.maturityDashboardFrontend | indent 4 }}
spec:
  selector:
    {{- include "global.labels" .Values.maturityDashboardFrontend | indent 4 }}
  type: {{ .Values.maturityDashboardFrontend.service.type}}
  ports:
    {{- include "service.servicePorts" .Values.maturityDashboardFrontend.service | indent 4 -}}
  {{- end }}
{{- end }}