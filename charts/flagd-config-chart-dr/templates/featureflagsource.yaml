apiVersion: core.openfeature.dev/v1beta1
kind: FeatureFlagSource
metadata:
  name: flag-source
  namespace: {{ .Release.Namespace }}
spec:
  sources:
    - source: {{ .Release.Namespace }}/{{ include "flagd-config-chart-dr.fullname" . }}-flags
      provider: kubernetes
