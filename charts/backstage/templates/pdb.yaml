apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "backstage.fullname" . }}-pdb
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "backstage.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "backstage.selectorLabels" . | nindent 6 }}
